# BS-typesctipt

 #### Install project:
 * ```git clone https://github.com/bl00dhound/BStypescript```
 * ```cd BStypescript```
 * ```npm install```
 
 #### Run:
 * ```npm run build```
 * ```npm start```
